// Pure arithmetic helpers. No state, no I/O — just `pub fn`s
// that main.rn and stats.rn can call as `math::add(a, b)`.

pub fn add(a: i64, b: i64) -> i64 { a + b }

pub fn mul(a: i64, b: i64) -> i64 { a * b }

pub fn square(x: i64) -> i64 { mul(x, x) }
